In case you realize you are in a WSL Linux environment, use the `WSL` tool to execute commands. In this case, when opening the browser, use the path /usr/local/bin/google-chrome-bridge.

If you're not in a WSL Linux environment, just use the basic windows commands to execute things